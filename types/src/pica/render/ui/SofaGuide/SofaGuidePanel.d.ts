import { BasePlaySceneGuide, UiManager } from "gamecoreRender";
import { IPos } from "utils";
export declare class SofaGuidePanel extends BasePlaySceneGuide {
    constructor(uiManager: UiManager);
    protected step1(pos: IPos): void;
}
