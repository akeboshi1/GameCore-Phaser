import { BaseGuide, UiManager } from "gamecoreRender";
export declare class ActivityGuidePanel extends BaseGuide {
    constructor(uiManager: UiManager);
    show(param?: any): void;
    private step1;
}
