import { BaseGuide, UiManager } from "gamecoreRender";
export declare class HomeGuidePanel extends BaseGuide {
    private homeBtn;
    constructor(uiManager: UiManager);
    show(param?: any): void;
    end(): void;
    private step1;
}
