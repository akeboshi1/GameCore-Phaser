export declare class PicaNewChatPanel extends Phaser.GameObjects.Container {
    private dpr;
    private key;
    constructor(scene: Phaser.Scene, width: number, height: number, key: string, dpr: number);
}
