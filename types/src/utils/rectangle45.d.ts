import Rectangle = Phaser.Geom.Rectangle;
export declare class Rectangle45 extends Rectangle {
    row: number;
    col: number;
    endRow: number;
    endCol: number;
    constructor(row: number, col: number, endRow: number, endCol: number);
}
