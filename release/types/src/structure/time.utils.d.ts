export declare class TimeUtils {
    static getDateFormat(time: number, haveday: boolean, color?: string): string;
    static stringFormat(num: number): string;
    static getBriefFormat(time: number): string;
    static dateFormat(fmt: string, date: Date): string;
}
