/**
 * 编辑器定义
 * author aaron
 */
export namespace EditorEnum {
  export enum Mode {
    BRUSH = "brush",
    ERASER = "eraser",
    MOVE = "move",
    ZOOM = "zoom"
}

  export enum Type {
    TERRAIN = 1,
    ELEMENT = 2
  }
}
