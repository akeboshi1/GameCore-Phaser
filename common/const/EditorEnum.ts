/**
 * 编辑器定义
 * author aaron
 */
export namespace EditorEnum {
  export enum Mode {
    BRUSH = "brush",
    ERASER = "eraser",
    MOVE = "move",
    ZOOM = "zoom",
    SELECT = "select"
}

  export enum Type {
    TERRAIN = "terrains",
    ELEMENT = "elements"
  }
}
