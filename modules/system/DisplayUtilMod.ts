/**
 * author aaron
 */
import BasicSprite from '../../display/BasicSprite';
import BaseSingleton from '../../base/BaseSingleton';
export class DisplayUtilMod extends BaseSingleton {

        /**
         * 将显示对象从父容器中移除
         * @param dis                   需要移除的显示对象
         * @param resetDisplayObject    是否将一些参数初始化
         */
        public removeFromParent(dis: BasicSprite, resetDisplayObject: Boolean = false): void {

        }
    }

